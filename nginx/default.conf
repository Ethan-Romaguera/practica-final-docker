server {
    listen 80;

    server_name localhost;

    location /images/ {
        alias /usr/share/nginx/html/images/;
        autoindex on;
    }

    location / {
        return 200 'Servidor Nginx de imágenes funcionando correctamente\n';
        add_header Content-Type text/plain;
    }
}